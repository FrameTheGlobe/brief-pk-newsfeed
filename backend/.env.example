# Copy this to .env for local backend development
# On Railway, set these in the Variables tab — do NOT commit .env

GROQ_API_KEY=gsk_your_key_here
NODE_ENV=production

# Optional: comma-separated list of allowed frontend origins (overrides defaults)
# ALLOWED_ORIGINS=https://briefpknews.xyz,https://www.briefpknews.xyz
